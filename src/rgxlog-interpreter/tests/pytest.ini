# another settings file, in addition to `conftest.py`
[pytest]
log_cli = 1
log_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s (%(filename)s:%(lineno)s)
log_cli_date_format = %Y-%m-%d %H:%M:%S
markers =
    long: annoying long tests
    engine: tests for engine only