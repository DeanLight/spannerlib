name: Deploy to GitHub Pages
on:
  push:
    branches: [deploy_documentation, moving_to_nbdev, master]
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: fastai/workflows/quarto-ghp@master